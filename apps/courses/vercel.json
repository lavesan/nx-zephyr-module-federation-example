{
  "buildCommand": "cd ../.. && pnpm install && npx nx build courses",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
