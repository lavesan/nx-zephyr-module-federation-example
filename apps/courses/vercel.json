{
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && npx nx build courses",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
