{
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && npx nx build profile",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
